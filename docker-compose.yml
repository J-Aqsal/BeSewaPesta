version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    # Volumes used for local development to sync code changes
    volumes:
      - .:/app
    restart: always
