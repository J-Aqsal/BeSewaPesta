services:
  web:
    build: .
    container_name: besewapesta-web-1
    expose:
      - "8000"
    env_file:
      - .env
    volumes:
      - .:/app
    restart: always

networks:
  default:
    name: sewapesta-network
    external: true